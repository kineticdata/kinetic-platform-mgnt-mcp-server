{
  "_comment": [
    "Cursor: place this at .cursor/mcp.json (project) or ~/.cursor/mcp.json (global).",
    "Credentials live in a gitignored .env loaded via --env-file, NOT in this file.",
    "Both paths must be ABSOLUTE. Requires Node >= 20.6.0.",
    "Leave KINETIC_MCP_MODE unset: slim mode (~10 tools) is the default and is",
    "what keeps Cursor under its ~40-tool ceiling."
  ],
  "mcpServers": {
    "kinetic-platform": {
      "command": "node",
      "args": [
        "--env-file=/ABSOLUTE/PATH/TO/kinetic-platform-mgnt-mcp/.env",
        "/ABSOLUTE/PATH/TO/kinetic-platform-mgnt-mcp/dist/index.js",
        "--stdio"
      ]
    }
  }
}
